title: First steps
description: How starting works, in three steps on a periwinkle sky — the sun rises a little higher at each one.
fields:
    heading: { type: text, label: Headline (first line) }
    headingItalic: { type: text, label: Headline (italic second line) }
    body: { type: textarea, label: Supporting copy, rows: 2 }
    steps:
        type: repeater
        label: Steps
        source: site.steps
        item_label: title
        sub_fields:
            title: { type: text, label: Step }
            body: { type: textarea, label: Description, rows: 3 }
