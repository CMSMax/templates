title: Header
description: The logo, the menu, the phone number, and the quote button at the top of every page.
fields:
    links:
        type: repeater
        label: Menu links
        source: site.nav_links
        item_label: label
        add_button_label: Add link
        sub_fields:
            label: { type: text, label: Label }
            href: { type: url, label: Link }
