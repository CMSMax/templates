title: Reviews
description: What members say — the first review set large, the rest in cards beside it, with the overall rating.
fields:
    eyebrow: { type: text, label: Section label }
    rating: { type: text, label: Rating }
    ratingNote: { type: text, label: Rating note }
    reviews:
        type: repeater
        label: Reviews
        source: reviews
        item_label: name
        add_button_label: Add a review
        sub_fields:
            quote: { type: textarea, label: Quote, rows: 3 }
            name: { type: text, label: Name }
            detail: { type: text, label: Detail }
