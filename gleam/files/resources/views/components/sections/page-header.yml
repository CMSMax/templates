title: Page header
description: The page title with a short introduction and a link to each service.
fields:
    eyebrow: { type: text, label: Small line above the heading }
    heading: { type: text, label: Heading }
    body: { type: textarea, label: Supporting copy, rows: 3 }
    items: { type: repeater, label: Services, source: services, item_label: name, sub_fields: { name: { type: text, label: Service }, slug: { type: text, label: Anchor } } }
