title: Hero
description: The opening split — the plum panel with the headline and inquiry button, and a photograph with this season's swatches pinned to it.
fields:
    kicker: { type: text, label: Small label above the headline }
    heading: { type: text, label: Headline }
    headingEmphasis: { type: text, label: Headline ending (set in italic) }
    body: { type: textarea, label: Supporting copy, rows: 3 }
    secondaryLabel: { type: text, label: Services link text }
    image: { type: image, label: Photo }
    imageAlt: { type: text, label: Photo description }
    showPalette: { type: toggle, label: Show this season's swatches on the photo }
    palette: { type: repeater, label: Swatches, source: site.palette, item_label: name, add_button_label: Add a swatch, sub_fields: { name: { type: text, label: Swatch name }, hex: { type: color, label: Color } } }
