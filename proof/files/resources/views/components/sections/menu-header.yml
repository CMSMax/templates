title: Menu header
description: The menu page's title, a short note, jump links to each menu group, and a photo.
fields:
    heading: { type: text, label: Title }
    body: { type: textarea, label: Supporting copy, rows: 3 }
    image: { type: image, label: Photo }
    imageAlt: { type: text, label: Photo description }
