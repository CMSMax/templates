title: Credentials
description: The practice's licences and certifications as a ruled list.
fields:
    heading: { type: text, label: Heading }
    items: { type: repeater, label: Credentials, source: site.credentials, item_label: name, sub_fields: { name: { type: text, label: Credential }, detail: { type: text, label: Detail } } }
